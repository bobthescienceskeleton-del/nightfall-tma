import { useStore } from '../store'

const RULES = [
  { ic: '🌙', t: 'Город с секретом', b: 'Каждому тайно достаётся роль. Несколько скрытых игроков это мафия, остальные честные горожане.' },
  { ic: '🔪', t: 'Наступает ночь', b: 'Мафия тихо выбирает, кого убрать, пока город спит.' },
  { ic: '🔎', t: 'Детектив', b: 'Каждую ночь проверяй одного игрока и узнавай, мафия ли он.' },
  { ic: '✚', t: 'Доктор', b: 'Каждую ночь защищай одного игрока. Если мафия ударит по нему, он выживет.' },
  { ic: '☀️', t: 'Приходит день', b: 'Город просыпается, видит, кого потерял, спорит и голосует против одного подозреваемого.' },
  { ic: '⚖️', t: 'Приговор', b: 'Кто набрал больше всех голосов, выбывает, и его настоящая роль открывается.' },
  { ic: '🏆', t: 'Как победить', b: 'Город побеждает, когда вычислит всю мафию. Мафия побеждает, когда её становится столько же, сколько горожан.' },
]

export function Rules() {
  const go = useStore(s => s.go)
  return (
    <div className="page rise">
      <div className="page-head">
        <button className="round-btn" onClick={() => go('home')}>‹</button>
        <h1>Как играть</h1>
      </div>
      {RULES.map((r, i) => (
        <div className="rule" key={i}>
          <div className="ic">{r.ic}</div>
          <div>
            <div className="rt">{r.t}</div>
            <div className="rb">{r.b}</div>
          </div>
        </div>
      ))}
    </div>
  )
}
